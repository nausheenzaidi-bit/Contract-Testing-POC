# Microcks application config overrides
# GraphQL mocking is enabled by default

# Allow larger artifact uploads (schema can be large)
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
